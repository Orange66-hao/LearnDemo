<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    

	<!-- 属性占位符（读取Properties配置文件） -->
	<!-- location：位置 -->
	<!-- ignore-resource-not-found：是否忽略不存在的资源（资源：配置项，键值对中的值） -->
	<!-- ignore-unresolvable：是否忽略无法解析的配置项 -->
	<context:property-placeholder location="classpath*:/config/db/config.properties" ignore-resource-not-found="true" ignore-unresolvable="true" />

	<!-- 扫描组件（扫描所有需要注入的组件） -->
	<context:component-scan base-package="net.boocu" use-default-filters="false">
		<!-- 例外 -->
		<!-- type：类型 -->
		<!-- expression：表达式 -->
		<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller" />
	</context:component-scan>

	<!-- 使用注解方式定义验证器 -->
	<mvc:annotation-driven validator="validator" />

	<!-- 静态资源访问 -->
	<mvc:resources location="/*.html" mapping="/*.html" />
	<mvc:resources location="/favicon.ico" mapping="/favicon.ico" />
	<mvc:resources location="/robots.txt" mapping="/robots.txt" />
	<mvc:resources location="/adminthemes/**" mapping="/adminthemes/**" />
	<mvc:resources location="/statics/**" mapping="/statics/**" />
	<mvc:resources location="/appframework2/**" mapping="/appframework2/**" />
	<mvc:resources location="/themes/**" mapping="/themes/**" />
	<mvc:resources location="/editor/**" mapping="/editor/**" />
	<mvc:resources location="/upload/**" mapping="/upload/**" />
	<mvc:resources location="/frontthemes/**" mapping="/frontthemes/**" />

	<!-- 拦截器 -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/account/**" />
			<mvc:mapping path="/borrowing/product/*/apply" />
			<mvc:mapping path="/investment/*/invest" />
			<bean id="userInterceptor" class="net.boocu.web.interceptor.MemberInterceptor" />
		</mvc:interceptor>
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<mvc:exclude-mapping path="/payment/**" />
			<bean id="tokenInterceptor" class="net.boocu.web.interceptor.TokenInterceptor" />
		</mvc:interceptor>
		<mvc:interceptor>
			<mvc:mapping path="/admin/**" />
			<bean id="logInterceptor" class="net.boocu.web.interceptor.LogInterceptor" />
		</mvc:interceptor>
		<mvc:interceptor>
			<mvc:mapping path="/admin/**" />
			<bean id="webContentInterceptor" class="org.springframework.web.servlet.mvc.WebContentInterceptor">
				<property name="cacheSeconds" value="0" />
			</bean>
		</mvc:interceptor>
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<bean id="openSessionInViewInterceptor" class="org.springframework.orm.jpa.support.OpenEntityManagerInViewInterceptor">
				<property name="entityManagerFactory" ref="entityManagerFactory" />
			</bean>
		</mvc:interceptor>
		
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<bean id="frontContextInterceptor" class="net.boocu.framework.web.FrontContextInterceptor" />
		</mvc:interceptor>
	 
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<bean id="frontLocaleInterceptor" class="net.boocu.framework.web.FrontLocaleInterceptor" />
		</mvc:interceptor>
		<!-- 商品列表拦截器 add by fang 20150909 -->
		<mvc:interceptor>
			<mvc:mapping path="/product*/list.jspx" />
			<mvc:mapping path="/product*/listAll.jspx" />
			<bean id="productControllerInterceptor" class="net.boocu.project.interceptor.ProductControllerInterceptor" />
		</mvc:interceptor>
		<mvc:interceptor>
			<mvc:mapping path="/product*/*.jhtml" />
			<bean id="productFlowInterceptor" class="net.boocu.project.interceptor.ProductFlowInterceptor" />
		</mvc:interceptor>
		
	</mvc:interceptors>

	<!-- 验证器 -->
	<bean id="validator" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean">
		<property name="validationMessageSource" ref="messageSource" />
	</bean>

	<!-- 视图解析器 net.boocu.web.freemarker.beeFreeMarkerViewResolver-->
	<!-- org.springframework.web.servlet.view.freemarker.FreeMarkerViewResolver -->
	<bean id="viewResolver" class="net.boocu.framework.freemarker.BooCuFreeMarkerViewResolver">
		<property name="contentType" value="text/html; charset=UTF-8" />
		<property name="suffix" value="${template.suffix}" />
	</bean>

	<!-- 多部件解析器 -->
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="defaultEncoding" value="UTF-8" />
	</bean>

	<!-- 异常解析器
	<bean id="exceptionResolver" class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
		<property name="defaultErrorView" value="error" />
	</bean>
	 -->
	 
    <import resource="spring-shiro.xml"/>
    <!--<import resource="spring-speed-up.xml"/>-->

</beans>
